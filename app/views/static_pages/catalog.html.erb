<%provide(:title, "Каталог")%>
<h1 class='h1 text-center'>Каталог</h1>
<p class='lead text-center'>
    Здесь вы найдете книги на свой вкус.<br>
    <%= link_to "Добавить", new_book_url, remote: true if !current_user.nil? && current_user.admin%>
</p>
<%= will_paginate @books%>
<div id="catalog">
    <%= render @books%>
</div>